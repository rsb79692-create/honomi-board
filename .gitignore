# サービスアカウント鍵は絶対にコミットしない
*firebase-adminsdk*.json
serviceAccount*.json
.env
.env.*
node_modules/
.firebase/

# GitHub Pages はリポジトリ直下を丸ごと配信するため、
# 旧版 HTML をコミットすると本番URLから閲覧できてしまう。必ず除外する。
_old/
_backup_*/

# 移行・巻き戻しの道具。GitHub Pages はリポジトリ直下を丸ごと配信するので、
# 公開したくないものはここへ足すこと。
